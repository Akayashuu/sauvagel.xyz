// Les articles nomment leurs icônes par chaîne : les données restent lisibles et
// sérialisables, et seule cette table importe des composants. Toute clé absente
// se rend sans icône plutôt que de casser le rendu.
import {
  Activity,
  Boxes,
  Clock,
  Container,
  Cpu,
  Database,
  FileCode,
  FlaskConical,
  Gauge,
  GitBranch,
  HardDrive,
  Layers,
  MemoryStick,
  Network,
  Package,
  Repeat,
  Rocket,
  RotateCcw,
  Scale,
  Server,
  ShieldCheck,
  Split,
  Timer,
  TrendingDown,
  TriangleAlert,
  Zap,
} from "lucide-svelte";

export const postIcons = {
  activity: Activity,
  boxes: Boxes,
  clock: Clock,
  container: Container,
  cpu: Cpu,
  database: Database,
  file: FileCode,
  flask: FlaskConical,
  gauge: Gauge,
  git: GitBranch,
  disk: HardDrive,
  layers: Layers,
  memory: MemoryStick,
  network: Network,
  package: Package,
  repeat: Repeat,
  rocket: Rocket,
  rollback: RotateCcw,
  scale: Scale,
  server: Server,
  shield: ShieldCheck,
  split: Split,
  timer: Timer,
  down: TrendingDown,
  warn: TriangleAlert,
  zap: Zap,
} as const;

export type PostIcon = keyof typeof postIcons;
